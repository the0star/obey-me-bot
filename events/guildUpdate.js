module.exports = {
  name: "guildUpdate",
  async execute(oldGuild, newGuild) {
    // mongodb $set: { info: newGuild }
    // console.log(JSON.stringify(oldGuild), JSON.stringify(newGuild));
  },
};
